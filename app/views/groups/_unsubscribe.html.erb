<%= form_for(current_user.active_relationships.find_by(group_id: @group.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unsubscribe", class: "btn" %>
<% end %>