<%= form_for(GroupMembership.find_by(user_id: current_user.id, group_id: @group.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unsubscribe", class: "btn" %>
<% end %>